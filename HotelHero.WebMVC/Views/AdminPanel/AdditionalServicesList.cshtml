@model IEnumerable<HotelHero.WebMVC.Models.AdditionalService>

@{
    ViewData["Title"] = "AdditionalServicesList";
}

<h1>AdditionalServicesList</h1>

<p>
    @* <a asp-action="CreateAdditionalService">Create New</a> *@
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <label asp-for="First().Name"></label>
            </th>
            <th>
                <label asp-for="First().Cost"></label>
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Name
                </td>
                <td>
                    @item.Cost
                </td>
                <td>
                    @* <a asp-action="EditAdditionalService" asp-route-id="@item.Id">Edit</a>
                    <a asp-action="DeleteAdditionalService" asp-route-id="@item.Id">Delete</a> *@
                </td>
            </tr>
        }
    </tbody>
</table>
